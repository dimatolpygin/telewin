# ============================================
# РЕПОЗИТОРИЙ ПУБЛИЧНЫЙ — секретам тут не место.
# Один коммит секрета = он в истории навсегда.
# ============================================

# Секреты и доступы
.env
.env.*
!.env.example
доступы.txt
docs/ACCESSES.md
.secrets/

# Скриншоты с ВИДИМЫМИ паролями клиента (лежат локально, в публичный репо нельзя):
# — FTP-строка masterberez.ru с логином и паролем открытым текстом
# — команда запуска Форы с PASSWORD=... в Планировщике
assets/Скрин_FTP_строка.png
assets/диагностика/02_задача_вкладка_Действия_команда.png

# Инструкция клиенту по выгрузке — по своей сути содержит боевой FTP-логин
# и пароль нашего сервера (иначе она бесполезна клиенту). Только локально.
docs/Инструкция_выгрузка_прайса.*
*.pem
*.key
id_rsa*
*.ppk

# Данные клиента (реальные прайсы: цены, остатки, коммерческая информация)
samples/
*.xls
*.xlsx

# Node
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Логи
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# Окружение разработчика
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Временное
tmp/
temp/
*.tmp
