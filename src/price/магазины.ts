/**
 * Магазины и их связь с колонками прайса.
 *
 * Единственное место, где «колонка в файле» превращается в «магазин». Дальше по
 * коду магазины живут строками в `stock`, и раскладка файла никого не волнует.
 */
import type { Роль } from './раскладка.js';

export interface ОписаниеМагазина {
  код: string;
  /** Как бот произносит магазин покупателю. Точные названия клиент ещё не прислал. */
  название: string;
  /** Из какой колонки прайса берётся остаток этого магазина. */
  роль: Extract<Роль, 'остатокМикрорайон' | 'остатокБерезовская'>;
}

export const МАГАЗИНЫ: readonly ОписаниеМагазина[] = [
  { код: 'микрорайон', название: 'Микрор-н', роль: 'остатокМикрорайон' },
  { код: 'березовская', название: 'п.ш. Березовская', роль: 'остатокБерезовская' },
];
